const initialState = {
    smurfs: [],
    isFetching: false,
    error: 'something went wrong'
}
const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default reducer